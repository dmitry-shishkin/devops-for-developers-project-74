services:
  app:
    image: dmitryshishkin/devops-for-developers-project-74:latest
    build:
      context: .
      dockerfile: Dockerfile.production
    command: make test
    ports:
      - "8000:8000"
